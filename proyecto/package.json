{
  "name": "aula-eficiente-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "firebase-admin": "^12.0.0",
    "@clerk/backend": "^1.0.0",
    "svix": "^1.30.0"
  }
}
