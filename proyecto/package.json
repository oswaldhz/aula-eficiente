{
  "name": "aula-eficiente-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "test": "node --test api/test/**/*.test.js",
    "test:watch": "node --test --watch api/test/**/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "firebase-admin": "^12.0.0",
    "@clerk/backend": "^1.0.0",
    "svix": "^1.30.0",
    "multer": "^1.4.5-lts.1"
  }
}
